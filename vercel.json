{
    "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
    "installCommand": "npm install && cd api && npm install && npx playwright install firefox && ls  node_modules/playwright-core/.local-browsers/ -R"
}